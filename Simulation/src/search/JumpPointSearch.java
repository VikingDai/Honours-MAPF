package search;

import utils.ISearch;

import java.util.List;

public class JumpPointSearch implements ISearch
{
    @Override
    public List<SearchNode> findPath(SearchNode start, SearchNode goal)
    {
        return null;
    }
}
